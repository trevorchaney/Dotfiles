# General aliases.
alias wtr="curl wttr.in"    # Get the local weather.
alias py="python3"
alias scim="sc-im"

# ls aliases
alias ls='ls -aG'
alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'

# Shortcut aliases
alias hd="cd /Volumes/MacHD"

# Use neovim for vim if present.
#command -v nvim >/dev/null && alias vim="nvim" vimdiff="nvim -d"
